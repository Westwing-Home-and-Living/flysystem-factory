Filesystem:
  adapter:
    localFS:
      type: "Local"
      root: "."
    sharedFS:
      type: "AwsS3"
      key:
      secret:
      region:
      bucket:
      prefix:
      options:
    ftp:
      type: "Ftp"
      host:
      #port:
      username:
      password:
      #ssl:
      #timeout:
      #root:
      #permPrivate:
      #permPublic:
      #passive:
      #transferMode:
      #ignorePassiveAddress:
